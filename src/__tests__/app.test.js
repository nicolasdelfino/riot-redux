// import "../tags/title.tag";

describe('main state', () => {

  // before(function(done) {
  //   riot.compile(function() {
  //     var html = document.createElement('test')
  //     document.body.appendChild(html)
  //     tag = riot.mount('*')[0]
  //     done()
  //   })
  // });
  //
  it('should mount the main app', () => {
    expect(0).toBeLessThan(1)
  })
  //
  // it('mounts the title tag', () => {
  //   let tag = riot.mount('title')[0]
  // })
})
